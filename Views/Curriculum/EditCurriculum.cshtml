@model SchoolSystem.Models.CurriculumManagement.Curriculum

<div class="container mx-auto px-4 py-8">
    <div class="max-w-2xl mx-auto bg-white shadow-xl rounded-xl overflow-hidden">
        <div class="bg-gradient-to-r from-indigo-50 to-blue-50 px-6 py-5 border-b border-gray-200">
            <h2 class="text-3xl font-extrabold text-gray-900">Edit Curriculum</h2>
        </div>

        <form asp-action="EditCurriculum" method="post" class="p-6 space-y-6">
            <input type="hidden" asp-for="CurriculumId" />

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                    <label asp-for="Curriculum_Code" class="block text-sm font-semibold text-gray-700 mb-2">
                        Curriculum Code <span class="text-red-500">*</span>
                    </label>
                    <input asp-for="Curriculum_Code" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm
                        focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
                           placeholder="e.g., CURR-001" required />
                    <span asp-validation-for="Curriculum_Code" class="text-red-600 text-sm mt-1 block"></span>
                </div>

                <div>
                    <label asp-for="Status" class="block text-sm font-semibold text-gray-700 mb-2">
                        Status <span class="text-red-500">*</span>
                    </label>
                    <select asp-for="Status" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm
                        focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500" required>
                        <option value="">Select Status</option>
                        <option value="Active">Active</option>
                        <option value="Inactive">Inactive</option>
                    </select>
                    <span asp-validation-for="Status" class="text-red-600 text-sm mt-1 block"></span>
                </div>
            </div>

            <div>
                <label asp-for="CurriculumName" class="block text-sm font-semibold text-gray-700 mb-2">
                    Curriculum Name <span class="text-red-500">*</span>
                </label>
                <input asp-for="CurriculumName" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm
                    focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
                       placeholder="e.g., Science Curriculum 2025" required />
                <span asp-validation-for="CurriculumName" class="text-red-600 text-sm mt-1 block"></span>
            </div>

            <div>
                <label asp-for="Description" class="block text-sm font-semibold text-gray-700 mb-2">
                    Description <span class="text-gray-500">(Optional)</span>
                </label>
                <textarea asp-for="Description" rows="4" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm
                    focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
                          placeholder="Additional details about this curriculum"></textarea>
                <span asp-validation-for="Description" class="text-red-600 text-sm mt-1 block"></span>
            </div>

            <div class="bg-gray-50 border border-gray-200 rounded-md p-4 mt-4">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <span class="text-gray-600 block mb-1">Created At</span>
                        <p class="font-medium text-gray-800">
                            @(Model.CreateAt.ToString("dd MMM yyyy HH:mm") ?? "N/A")
                        </p>
                    </div>
                    <div>
                        <span class="text-gray-600 block mb-1">Last Updated</span>
                        <p class="font-medium text-gray-800">
                            @(Model.UpdateAt?.ToString("dd MMM yyyy HH:mm") ?? "N/A")
                        </p>
                    </div>
                </div>
            </div>

            <div class="flex items-center justify-between mt-6">
                <div>
                    <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-6 rounded-md
                        transition duration-300 ease-in-out transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-indigo-500">
                        Save Changes
                    </button>
                    <a asp-action="CurriculumManagement" class="ml-4 text-gray-600 hover:text-gray-900">Cancel</a>
                </div>
                <a asp-action="Details" asp-route-id="@Model.CurriculumId"
                   class="text-indigo-600 hover:text-indigo-800 hover:underline">
                    View Details
                </a>
            </div>
        </form>
    </div>
</div>
